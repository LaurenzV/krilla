pub use crate::object::{color::rgb::Color, image::Image, page::Page};

pub use crate::{
    configure::*,
    document::*,
    font::Font,
    geom::*,
    object::*,
    serialize::{SerializeSettings, SvgSettings},
};
